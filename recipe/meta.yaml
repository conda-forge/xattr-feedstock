{% set name = "xattr" %}
{% set version = "0.9.4" %}
{% set bundle = "tar.gz" %}
{% set hash_type = "sha256" %}
{% set hash = "eec15abe558ea6363c6bf4b7c40c8924f7be65b7a5d251c93e0a1278d8481bbe" %}
{% set build = 0 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.{{ bundle }}
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ bundle }}
  {{ hash_type }}: {{ hash }}

build:
  skip: True  # [win]
  entry_points:
    - xattr = xattr.tool:main

  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools
    - toolchain
    - cffi >=1.0.0

  run:
    - python
    - cffi >=1.0.0

test:
  imports:
    - xattr

  commands:
    - xattr --help

about:
  home: https://github.com/xattr/xattr
  license_file: LICENSE.txt
  license: MIT
  license_family: MIT
  summary: 'Python wrapper for extended filesystem attributes'
  dev_url: https://github.com/xattr/xattr

extra:
  recipe-maintainers:
    - pmlandwehr
