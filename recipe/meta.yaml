{% set name = "xattr" %}
{% set version = "0.9.8" %}
{% set bundle = "tar.gz" %}
{% set hash = "bf11c8c857215e3ef60b031e7807264f30af4348d7565a7e9b8dca70593753c7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.{{ bundle }}
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ bundle }}
  sha256: {{ hash }}

build:
  skip: true  # [win]
  entry_points:
    - xattr = xattr.tool:main

  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv

requirements:
  build:
    - {{ compiler('c') }}

  host:
    - python
    - pip
    - cffi >=1.0.0

  run:
    - python
    - cffi >=1.0.0

test:
  imports:
    - xattr

  commands:
    - xattr --help

about:
  home: https://github.com/xattr/xattr
  license_file: LICENSE.txt
  license: MIT
  license_family: MIT
  summary: Python wrapper for extended filesystem attributes
  dev_url: https://github.com/xattr/xattr

extra:
  recipe-maintainers:
    - pmlandwehr
