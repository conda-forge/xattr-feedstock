{% set name = "xattr" %}
{% set version = "0.9.3" %}
{% set bundle = "tar.gz" %}
{% set hash_type = "sha256" %}
{% set hash = "cd010284c816bad19d271f33c14666583669455779d3f7cb0047f4368e49f80b" %}
{% set build = 0 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.{{ bundle }}
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ bundle }}
  {{ hash_type }}: {{ hash }}

build:
  skip: True  # [win]
  entry_points:
    - xattr = xattr.tool:main

  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools
    - toolchain
    - cffi >=1.0.0

  run:
    - python
    - cffi >=1.0.0

test:
  imports:
    - xattr

  commands:
    - xattr --help

about:
  home: https://github.com/xattr/xattr
  license_file: LICENSE.txt
  license: MIT
  license_family: MIT
  summary: 'Python wrapper for extended filesystem attributes'
  dev_url: https://github.com/xattr/xattr

extra:
  recipe-maintainers:
    - pmlandwehr
